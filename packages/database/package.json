{
  "name": "@bokari/database",
  "version": "1.0.0",
  "private": true,
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/esm/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:prisma && yarn build:ts",
    "build:prisma": "yarn generate:schema && yarn generate:client",
    "build:ts": "tsc --project tsconfig.cjs.json && tsc --project tsconfig.esm.json",
    "generate:schema": "prisma introspect --force && prisma-schema-transformer prisma/schema.prisma",
    "generate:client": "prisma generate"
  },
  "devDependencies": {
    "@prisma/cli": "^2.7.0",
    "prisma-schema-transformer": "^0.5.0",
    "typescript": "^4.0.3"
  },
  "dependencies": {
    "@prisma/client": "^2.7.0"
  }
}
